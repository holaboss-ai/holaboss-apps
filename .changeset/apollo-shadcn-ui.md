---
"apollo": patch
---

fix: web UI redesigned with shadcn primitives (hairline-divider activity feed, header connection Badge, Notion-style smart timestamps) and `__root.tsx` migrated to the current TanStack Start root API so the SSR HTML actually emits the stylesheet link.
